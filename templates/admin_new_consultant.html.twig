{% extends 'base.html.twig' %}

{% block title %}TRT-Conseil - Nouveau consultant{% endblock %}

{% block body %}
    {{ form_start(form, {'attr': {'class': 'form', 'id': 'user-form', 'style': 'margin-bottom: 16vh;'}}) }}
        <h1 class="form-title">Création d’un compte consultant</h1>
        {% for field in form %}
            {{ form_row(field) }}
        {% endfor %}
        <input type="submit" class="button form-btn" value="Je crée le compte" />
    {{ form_end(form) }}

    {% for flashMessage in app.session.flashbag.get('success') %}
        <div class="alert alert-success">
            {{ flashMessage }}
        </div>
    {% endfor %}

    {% for flashError in app.session.flashbag.get('error') %}
        <div class="alert alert-danger">
            {{ flashError }}
        </div>
    {% endfor %}
{% endblock %}
