{% extends 'base.html.twig' %}

{% block title %}Edition du profil - Candidat{% endblock %}

{% block body %}
    {{ form_start(form, {'attr': {'class': 'form', 'id': 'new-jobposting-form', 'style':'margin-bottom:12vh;'}}) }}
        <h1 class="form-title">Votre annonce</h1>
        {% for field in form %}
            {{ form_row(field) }}
        {% endfor %}
        <input type="submit" class="button form-btn" value="Poster l'annonce" />
    {{ form_end(form) }}
    <div class="return-button">
        <a href="{{ path('recruiter_dashboard') }}" class="link return-button-link">Retour au tableau de bord</a>
    </div>
{% endblock %}